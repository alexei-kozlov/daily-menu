<h1 class="text-center mb-4" style="font-family: Pacifico, Arial, sans-serif;">Заказы</h1>
<% @orders.each do |order| %>
  <%= link_to order_path(order), :class => 'order-item container col-md-6 col-10 mx-auto p-0 d-block h3' do %>
    <div class="wrapper alert alert-dark d-flex pl-2 flex-wrap justify-content-between align-items-center border p-0">
      <span><%= l order.created_at %></span>
      <%= content_tag(:div, content_tag(:i, nil,:class => 'fas fa-info'),
      :class => 'btn-inform btn btn-primary m-1 d-block') %>
    </div>
  <% end %>
<% end %>
<div class="mx-auto text-right col-md-6 col-10 p-0 mt-4">
  <%= link_to "Создать заказ", new_order_path, :class => 'btn btn-success col-12' %>
</div>